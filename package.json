{
  "name": "filipstal",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:food": "npm run dev --workspace=food",
    "dev:www": "npm run dev --workspace=www",
    "build:food": "npm run build --workspace=food",
    "build:www": "npm run build --workspace=www"
  }
}
